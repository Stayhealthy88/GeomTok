# GPL Tokenizer — 연구 연속 보고서 (2026-06-10, 갱신 06-11)

13개 에이전트 정밀 분석 + 적대적 비판 3건(압축·신규성·실험설계) + 공정 베이스라인 실험.

---

## 0. 비판적 재설정 (2026-06-11) — 무엇이 죽고 무엇이 살아남았나

E1~E5 완료 후 3개 적대 비판 에이전트 + 공정 베이스라인 실험으로 전 주장을 재검증했다. **결과: 생성 모델 주장은 대부분 무너졌고, 토크나이저 본체만 살아남았다. 이게 오히려 논문을 정직하고 강하게 만든다.**

### 철회·정정 목록 (실측 근거)

| 항목 | 기존 주장 | 실측 후 | 근거 |
|---|---|---|---|
| **압축률** | BPE 대비 7.49–7.65배 | **동일 어휘(5,561) SVG 학습 BPE 대비 3.54배** (비트/아이콘도 3.54배) | cl100k는 100k 어휘·비도메인 스트로맨. 공정 BPE는 935 tok/icon, GPL-L1 264 |
| **HMN 기하 초기화가 학습 가속** | val_loss 4.9→3.4 개선은 기하 구조 덕 | **철회** — `random_unitnorm`(기하 0, 노름만 일치)이 갭 ~100% 닫음(val 3.41). weight tying 하 임베딩 노름 아티팩트 | random 노름 11.3 vs HMN 1.0 → 출력층 포화 |
| **L2 도형 압축 5.6배** | 핵심 기여 | 실데이터 L2 **발화율 0.0%** — 실세계 path 아이콘에 4-Bézier 원 시그니처 없음 | 400 아이콘 측정 |
| **L3 공간 지능 최대 15배** | 핵심 기여 | 실데이터 L3 **발화율 14.5%, 압축 기여 +5.2%** | 토이 사례 한정 |
| **유효율(HMN이 깎음 등)** | 일련의 갭 | E4에서 측정 아티팩트로 판명(도형 무시 검증기). 단 100% 포화 지표는 변별력 0 | |
| **밀도 적응 트리 1.58px** | uniform과 근소차 | **무효** — test 분할에서 thr·level 그리드서치 후 같은 점에서 평가(fit-on-test) | exp_e3_density_tree |
| **다양도 0.074→0.081 개선** | 제약 디코딩 효과 | σ≈0.006 노이즈 내. 게다가 희소 잉크(2%)에서 해밍 지표는 붕괴 탐지 불가 | |

### 결정적 신규 발견 (살아남았고, 더 강함)

**학습 BPE가 hand-crafted L2/L3 매크로를 이긴다 — 그런데 GPL-L1이 그 BPE의 더 좋은 기질(substrate)이다.** L1 토큰 스트림 위에 BPE(+1.5k 머지) 학습 시 264→**151 tok/icon (−43%)** — L2/L3 수작업 검출기(+5%)를 압도. 동시에 원문 SVG 위 BPE는 같은 어휘로 935 tok에 그침. 즉 **"기하 프리미티브 토큰화가 학습 압축의 6.2배 더 나은 기질"**이 새 헤드라인. 수작업 매크로는 버리고, L1 위 학습 머지로 전환.

### 살아남은 자산 (출판 가능 핵심)

1. **실데이터 파싱·왕복 100%** (2,682 실세계 아이콘) — 모델·분할 무관 깨끗한 토크나이저 본체 결과
2. **스칼라 고정소수점 코덱**(E1) — 무파라미터, r 오차 0.04px
3. **uniform-L6이 적응 ARCS를 압도**(E3) — 오염된 이점을 줘도 uniform 승(부정 결과, 정직)
4. **공정 압축 3.54배** — HiVG(2.7배)·OmniSVG 균일격자 대비 여전히 우위, 정직한 베이스라인
5. **GeomTok-Eval 프로토콜** — 단, path 좌표 충실도로 확장 필수(현재 path-only 아이콘에 무한대 반환)

### 새 논문 논지 (CPU 규모 방어 가능) — 문헌 게이트로 재구성 (2026-06-11)

**경고**: "기하 프리미티브 위 BPE 머지"·"프리구조화가 raw보다 BPE에 유리"는 **HiVG(2604.05072)·GeoBPE(2511.11758)에 정면 선점됨**. HiVG는 이미 기하 세그먼트 토큰 위 빈도 기반 머지(M=500)를 수행, 우리 직관을 명시적으로 서술. 따라서 "최초"·"프리구조화 우위" 표현 **전면 금지**.

재구성된 방어 가능 신규성 3종:
> (i) **학습 비제약 머지(GPL)가 HiVG식 구조-제약 규칙 머지를 이긴다** — 어떤 SVG 논문도 안 한 직접 비교 (실험 A)
> (ii) **수작업 도형/공간 매크로는 실세계에서 0% 발화** — HiVG가 안 한 부정 결과 (F3에서 확정)
> (iii) **GeomTok-Eval — 생성기 분리형 렌더 기반 내재 프로토콜** (명칭 색인 0건, 독립 산물)

HiVG를 주 베이스라인으로 인용·격파. 6.21배 숫자는 "새 현상"이 아니라 "HiVG가 빠뜨린 char-BPE-vs-프리미티브-BPE ablation 충원"으로 프레임. 제출 전 arXiv 최종 색인 확인 1회 필수.

생성 모델 주장(HMN·유효율·다양도)은 전부 합성 전용·교란·포화로 **현재 미지지** → 보류하거나 실데이터 학습 후 재측정.

### 문헌이 요구하는 추가 실험 (F5 전 삽입, 전부 CPU)
- ~~**A. HiVG 규칙 머지 vs GPL 학습 머지**~~ — **완료 (2026-06-11), 논지 i 입증·적대 검증 통과**. 동일 greedy BPE·동일 머지 수를 char/L1-비제약/L1-경계제약/L1-충실HiVG(명령루트 세그먼트) 4 기질에 적용(train 1200, test 400). 결과(tok/icon @1000머지): **char-BPE 824 ≫ L1-비제약 172 < L1-경계 222 < L1-충실HiVG 236**. ① 비제약 학습 머지가 충실 HiVG식 구조 제약을 37% 우위로 이김(172 vs 236) — 명령 경계 넘는 반복 패턴 발견을 구조 제약이 금지해 손해 ② bits/icon(order-0)도 동일 순위(1690<1937≪7245)로 정보론적 확증 ③ 동일 총어휘 대조에서도 char 대비 L1 3.2배 우위. **검증 정정**: 초기 "동일 어휘"→"동일 머지 수"로 표기 수정, 첫 시도의 무한어휘 그룹핑 폐기. **주의(bits 포화)**: L1 bits는 100머지 후 거의 평탄 — BPE 머지는 정보 재포장이라 tokens/icon이 실이득을 과대표시. 논문 표는 bits 병기. HiVG가 격파 대상 주 베이스라인으로 확정
- **B. 머지 예산(N) 스윕** — char-BPE에도 동일 머지 예산. 단일점 6.21배는 "예산 불공정" 공격 대상
- **C. CNM 연속 회귀 헤드 arm** (F5에 포함) — 좌표 토큰 폐기 진영 응수
- **D. 좌표 충실도 분포·꼬리** — 인지 가능 오차(>2px) 비율
- **E. StrokeNUWA 14배 VQ 점** — 압축-충실도 산점도에 손실 VQ 상한 표시
- **F. 어휘 크기 통제 컬럼** — 모든 arm 동일 5,561 명시

---

## 1. 현재 위치 — 솔직한 진단

**좋은 소식: 핵심 아이디어는 독립적으로 검증됐고, 분야의 최전선과 정확히 같은 문제를 공격하고 있다.**

- **HiVG** (arXiv:2604.05072, 2026.4, LLM4SVG 그룹) — GPL과 동일한 진단(좌표 환각, 토큰 중복)에 동일 계열 해법(atomic+segment 토큰, HMN 임베딩 초기화, 커리큘럼 학습). 가장 가까운 경쟁자이자 **반드시 인용·비교해야 할 대상**.
- **차별점은 명확함**: 공개 문헌에 쿼드트리 적응 해상도 좌표(ARCS)를 쓰는 SVG 토크나이저는 아직 없다. HiVG은 균일 양자화. 메시 분야 BPT(arXiv:2411.07025)의 블록+오프셋 계층 좌표가 기하 도메인 최고 압축(~75%)을 기록 — ARCS와 정확히 같은 구조의 독립 검증.
- L1/L2/L3 계층은 HNC-CAD(ICML 2023, 3단계 코드 트리), SuperBPE(COLM 2025), VFig(2026, 프리미티브 치환 검증)이 모두 지지하는 방향.
- 5.5k 어휘는 검증된 최적 구간(HiVG ~2.95k, CAD-Tokenizer 2k, VQ-SGen 8k).

**나쁜 소식: 현재 증거는 출판 기준 미달이고, 코드 감사에서 실질 결함이 확인됐다.**

| 결함 | 증거 | 심각도 |
|---|---|---|
| 스칼라 인코딩 파탄 | 반지름·간격을 (v,v) 2D 양자화 → r=20이 37.5로 복원(87% 오차), 7개 원이 38개로 복원(REPEAT_N) | **치명** |
| 평가기 무력 | 무작위 토큰열이 structural 1.000 점수, 정답이 0.987 | **치명** |
| 가짜 BPE 베이스라인 | chars/4 휴리스틱, 실제 토크나이저 미사용 | 치명→**해결됨(아래 실험)** |
| 스타일/transform 전부 소실 | fill/stroke 하드코딩, transform 미처리 → 실세계 SVG 파싱 불가 | 높음 |
| 죽은 파라미터 14.7% | TransformerDecoder에 zero memory cross-attention | 중간 |
| 합성 데이터·실데이터 규약 불일치 | synthetic_dataset 반지름 인코딩 ≠ CompositeTokenizer 규약 | 높음 |
| 예약 ID 디코드 크래시 | decode_token_id가 ID 5–9, 71–79에서 ValueError → 53.3% 유효율 왜곡 | 중간 |

## 2. 주장 검증 결과

| 주장 | 판정 | 근거 |
|---|---|---|
| BPE 대비 2–3× GID | **실측으로 보수적임이 판명** | 실제 cl100k(GPT-4) 측정: 단일 도형 5.4–6.2×, 패턴 7.2–8.8×, 곡선 패스 18.4×, 합계 **7.8×**. 단, 토이 도메인 한정 — 실세계 코퍼스 측정 필요 |
| 최대 15× 압축 | 동일 — 토이에서 실측 18.4×까지 확인 | 실세계 SVG에선 파서가 실패하므로 미검증 |
| 무손실 round-trip | **과장** | 좌표는 (cell/2)√2 한계 내 양자화(레벨6 3.3px / 기본 53px), 스칼라·스타일은 대량 손실. "무손실" 문구 폐기 필요 |
| HMN 0.52 코사인 | **과장** | 0.52는 d=128 설정값(d=256은 0.50). 인접−원거리 갭 ~0.05, 주기-1 앨리어싱으로 비단조 감쇠. "학습 도움"은 본 보고서 ablation 전까지 무실험 |
| HMN이 학습을 돕는다 | **절반만 사실 (신규 실험)** | 3시드 ablation: val_loss 3.77±0.04 (HMN) vs 4.91±0.11 (random) — 명확한 개선. **그러나 유효 SVG 생성률은 67.8% vs 82.2%로 역전** — 코드 감사가 예측한 weight-tying 노름 편향(좌표 토큰 노름 ~1.0 vs EOS ~0.03 → EOS 억제)과 일치. n=90이라 추가 확인 필요 |
| 인용 4건 실존 | **지지** | HiVG·StrokeNUWA·LLM4SVG·VectorGym 모두 실존(2건 동료심사, 2건 2026 프리프린트) |

## 3. 최신 연구 핵심 (필독)

1. **HiVG** (arXiv:2604.05072) — 최근접 경쟁. 2.7× 압축, SSIM 0.896. 균일 격자 ↔ ARCS 차별화 축
2. **CNM** (arXiv:2602.02820) — 정반대 진영: 좌표 토큰 폐기, 연속 회귀 헤드. **반드시 응수해야 할 도전**
3. **OmniSVG** (NeurIPS 2025) — (x,y)→단일 토큰, 200×200 격자, 비교 기준점
4. **StrokeNUWA** (ICML 2024) — VQ 토큰 6.9% 압축, 94× 속도 — VQ 상한선
5. **RLRF** (arXiv:2505.20793) — 렌더링 RL: MSE 8.6→1.0. 토크나이저 후 다음 단계의 표준
6. **FoNE** (ICLR 2026) — Fourier 단일 토큰 수치 임베딩 — HMN 개선 레시피
7. **NTL** (ICML 2025) — 수치 거리 가중 보조손실 — 즉시 이식 가능
8. **Pointer-CAD** (2026) — 양자화 위상 오류 경고 — 공유 끝점은 포인터 참조
9. **벤치마크**: SVGEditBench V1/V2(저비용), SVG-Bench, MMSVG-2M, SVGenius, VectorGym(2026 최고난도)
10. **데이터**: FIGR-8(150만 단색 아이콘 — 우리 체급 최적), SVG-Stack 2.1M
11. **방법론**: NAACL'24 — 토크나이저만 바꾼 동일 소형 모델 비교가 표준 프로토콜. 압축률 단독은 증거로 불인정(ACL'24)
12. 토크나이저-프리(BLT/H-Net)는 ≥1B 규모 전용 — 소형 체급에서 GPL 노선 유효

## 4. 연구 로드맵 v0.6+ (우선순위순)

1. ~~**E1. 스칼라 파탄 수정**~~ — **완료 (2026-06-10)**. 스칼라 고정소수점 코덱: level-6 64×64 격자를 12-bit 숫자 체계로 재해석(qx·64+qy), 어휘 추가 0개. 결과: r=20 오차 17.5px→0.04px, REPEAT 7→7 무손실(이전 38), rect/ellipse 크기 ≤0.04px. 적대적 검증으로 카운트 폭주·레벨 무시·NaN 가드 3건 보강. GeomTok-Eval 프로토콜 시드(`evaluation/value_fidelity.py`) 구현 — count_acc 100%, 남은 병목은 직선 콘텐츠의 위치 양자화(최대 17.5px, → E3에서 균일 vs 적응 격자 ablation으로 직결)
2. **E2. 실데이터 정복** — **진행 중 (2026-06-11)**. 딥리서치(25개 출처 클레임; 검증 단계는 세션 한도로 미완 — 단일 출처 증거로 취급): ① 내재 평가 프로토콜 선행작 없음 → GeomTok-Eval 갭 유효. ② transform 표준은 평탄화-후-폐기(OmniSVG=picosvg, DeepSVG는 g 상속 미지원) — 우리 네이티브 평탄화가 표준과 일치하며 g-상속까지 지원해 우월. ③ 단색 도메인 fill 표준도 폐기(IconShop·DeepSVG) — 현 동작 선례 일치. ④ 쿼드트리 좌표 선점 없음(OmniSVG v2 기준). ⑤ 코퍼스: FIGR-8 HF 미공개 → StarVector svg-icons(SVG-Bench 분할, 실세계 2,682 test)로 측정. 구현 완료: affine transform 평탄화+그룹 상속, defs/clipPath 제외, viewBox 정규화(등방·중앙), 압축 arc 플래그 파싱. 테스트 200/200
3. ~~**E3. 좌표 체계 본 실험**~~ — **완료 (2026-06-11)**. 실세계 400 아이콘(좌표 27,414개), 동일 토큰 수 통제 비교: **균일 64×64가 적응 ARCS를 압도** (평균 오차 1.78 vs 14.75px, 렌더 150개 SSIM 0.929 vs 0.846, inkIoU 0.655 vs 0.284). 곡률 기반 분할이 직선 영역을 방치하는 게 원인. 밀도 기반 코퍼스 전역 트리(L7, thr40)도 평균 1.58로 근소 우위지만 꼬리 13px로 패배. OmniSVG식 200×200은 오차 0.57px이나 어휘 40k 필요 — 어휘 효율(1/(슬롯×오차))은 uniform 2.39 > omni 1.60 > adaptive 0.29. **조치: uniform-L6을 토크나이저 기본값으로 전환**(`uniform_coords=True`, 적응 트리는 연구 경로로 유지) — 입력 의존 토큰화 약점도 동시 해소. GeomTok-Eval attr_max_err 17.5→2.34px, 압축 7.49배 유지. 논문 논지 수정: "적응 쿼드트리 우월" → "균일 격자 + 스칼라 고정소수점이 어휘 효율 최적, 적응 트리는 곡률 신호로는 기각(부정 결과로 보고)"
4. ~~**E4. 평가기 교체 + 문법 제약 디코딩**~~ — **완료 (2026-06-11)**. ① `RenderEvaluator`(resvg 렌더 성공률·잉크 비율·비퇴화율·잉크마스크 다양도)로 게임 가능 평가기 대체. ② FSA 문법 마스크 디코딩(`training/grammar.py`) 기본 적용 — 문법 유효 100% 보장, 다양도 0.074→0.081. ③ **역사적 미스터리 해결**: v0.5의 53%, E1의 68% vs 82%, E5의 70% vs 77% "유효율 갭"은 전부 path-만 인정하던 검증기가 `[CIRCLE][중심][r]` 도형 출력을 무효 처리한 측정 아티팩트 — 도형 인지 검증 후 전 arm 100%. HMN이 유효율을 깎는다는 결론 철회: HMN 모델이 도형 토큰을 더 잘 생성해 벌점을 받던 것
5. ~~**E5. HMN 재설계**~~ — **완료 (2026-06-11)**. HMN v2 구현: 노름 균등화(전 토큰 1.0) + 주파수 0.25~64 단조 감쇠(앨리어싱 제거) + level 마커 제거. 인접−원거리 갭 0.157→0.281 (1.8배). 4-arm×3-seed×생성100: val_loss random 4.42 > v1 3.53 > **v2 3.43**(최고). 유효율 v2 69.7% vs random 77.0%±10.4 — 갭 14pt→7pt 축소, 1σ 중첩. **노름 편향 가설 기각**: 균등화로 유효율 미회복 → 잔여 갭은 노름이 아니라 손실 우위 모델이 좌표-밀집 시퀀스를 더 시도하는 데서 발생(유효율은 저난도 출력일수록 유리한 저기준 지표 — E4 평가기 교체가 진짜 해법). NTL(λ=0.5)은 이 규모에서 무이득(3.49)·향후 실데이터에서 재시험
6. **E6. 패키징** (1주) — 디렉터리 `gpl_tokenizer/` 정규화, pytest 마이그레이션, zero-memory 제거(1.79M→1.53M)

## 4b. 로드맵 v0.7 — 비판 반영 재설정 (우선순위순)

기존 E6 이후를 폐기하고, 살아남은 논지("기하 토큰 = 학습 압축의 더 좋은 기질")를 증명하는 최소 실험으로 재편.

1. **F1. GeomTok-Eval path 확장** (필수, 반나절) — `_extract_values`를 path 좌표(음수·지수 포함)까지 측정하도록 확장. 현재 실세계 아이콘에 ∞ 반환 → 무용. 이게 없으면 어떤 비교표도 못 만듦
2. **F2. 공정 베이스라인 표** (필수, 1일) — GeomTok-Eval에 ① 원문 char-BPE(5,561) ② IconShop식 100×100 격자 ③ OmniSVG식 200×200 ④ GPL-L1 ⑤ GPL-L1+학습BPE를 모두 래핑, svg-icons + FIGR-8 2코퍼스. tokens/icon + bits/icon + 렌더 SSIM/inkIoU. cl100k는 표에서 강등(참고용 각주)
3. ~~**F3. 손실 감사**~~ — **완료 (2026-06-11)**. 실데이터 150 아이콘 렌더: **L1 SSIM 0.929 / inkIoU 0.655 = 3.54배 압축의 정직한 충실도 비용**. L2는 도형 미발화로 L1과 완전 동일(공정 렌더 경로에서 0.435는 stroke-width 아티팩트로 판명·정정) — 수작업 도형 토큰은 실세계 아이콘에 압축·충실도 둘 다 무변화. 압축표는 이 충실도와 나란히 보고할 것
4. **F4. HMN 정직 재실험** (필수, 1시간) — `random_unitnorm` + untied 헤드 대조군 추가, LR 스윕. 잔여 효과 없으면 "HMN" 전면 철회, "weight tying 노름" 각주로 강등
5. ~~**F5. 토크나이저-스왑 다운스트림**~~ — **완료 (2026-06-11), 핵심 발견**. 동일 2.5M 바닐라 디코더를 실세계 아이콘(1045 train/213 test, ≤160 L1토큰)에 토크나이저만 바꿔 학습(NAACL'24), held-out bits/icon 3-seed. 결과: **char-BPE 775±6 > L1+BPE 666±2 > L1 576±4**(낮을수록 우월, 격차 25–30σ). **두 발견**: ① 기하 프리미티브 토큰(L1)이 다운스트림 모델링 최고 — char-BPE보다 26% 우월(합성 전용 구멍 메움) ② **압축 ≠ 모델링**: L1+BPE는 압축 1.8배(58.8 vs 103.7 tok)인데 다운스트림 더 나쁨 — 머지가 토큰당 엔트로피를 높여 net-negative(11.3 vs 5.6 bits/tok). 게이트 질문 "내재가 외재를 예측하는가"에 깨끗한 반례. **헤드라인 재확정**: "기하 토큰화가 SVG 다운스트림 모델링의 최적 기질, 그리고 압축 우위는 모델링 우위를 보장하지 않는다." **생성 품질 보강(완료, 200 생성/arm)**: 렌더 성공률 char-BPE **0%**(텍스트 모델은 깨진 XML만) / L1 **84%** / L1+BPE 35%. FID는 L1 0.197 ≈ L1+BPE 0.194(유효 표본 한정)이나 **L1이 생성 신뢰도 2.4배**. 다양도 0.89/0.92(모드붕괴 아님), 신규성 0.84(암기 아님). 결론 강화: 기하 토큰이 생성에 **필수**(텍스트 0%), 공격적 머지는 압축은 좋아도 생성 유효성을 망침(84→35%)
6. **F6. κ-곡률/연속성 토큰 사망 검사** — IconShop 대비 순수 오버헤드인 30–55 토큰이 다운스트림에 기여하는지. 무기여면 제거
7. ~~E6 패키징~~ — F1–F5 통과 후 마지막에

**Kill criterion**: F5에서 L1+BPE ≈ L3 다운스트림이면 L2/L3 폐기하고 프로토콜/벤치마크 논문으로 선회(그래도 출판 가능 — NAACL'24 토크나이저-스왑 방법론 + 부정 결과들 + GeomTok-Eval).

## 5. 출판 전략 — 정직한 버전

CS에는 노벨상이 없고 GPL은 ML이라 ACL/EMNLP/CVPR/NeurIPS 계열이 무대다 — 이게 비관이 아니라 정상 경로다.

1. **헤드라인 후보**: "기하 프리미티브 토큰화는 학습 BPE의 6.2배 나은 기질" (F5가 다운스트림에서 확증하면 본 논문, 아니면 분석/워크숍)
2. **백업 자산** (F5 실패해도 출판 가능): 실데이터 파싱 100% + 스칼라 코덱 + uniform-vs-adaptive 부정 결과 + GeomTok-Eval 프로토콜 → 분석 트랙
3. **금지**: cl100k 7.6배 압축, HMN 기하 가속, L2 5.6배, L3 15배 — 전부 철회됨. 정직한 3.54배·6.2배 기질만 사용
4. **명칭**: "GPL"은 라이선스와 충돌 — 출판 전 개명 권고. 라이선스: proprietary → 코드 공개 없이는 채택 불가
